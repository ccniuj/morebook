<p id="notice"><%= notice %></p>

<h1>書櫃列表</h1>

<table>
  <thead>
    <tr>
      <th>名稱</th>
      <th>管理</th>
    </tr>
  </thead>

  <tbody>
    <% @shelves.each do |shelf| %>
      <tr>
        <td><%= shelf.name %></td>
        <td>
          <%= link_to '編輯', edit_dashboard_admin_shelf_path(shelf) %>
          <%= link_to '刪除', shelf, method: :delete, data: { confirm: '你確定嗎？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新增書櫃', new_dashboard_admin_shelf_path %>
