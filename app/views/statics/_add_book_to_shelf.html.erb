<% if @book_db.nil? %>
  <%= form_tag add_book_to_shelf_statics_path, :method => 'post' do %>
    <%= hidden_field_tag 'isbn', @book[:isbn] %>
    <%= hidden_field_tag 'book_data_url', request.original_url %>
    這本書不在書櫃裡, <%= submit_tag '新增到我的書櫃' %>
  <% end %>                      
<% end %>

<!-- if current_user
  if book_in_db
    if book_in_shelf
    else
    end
  else

  end
else
    
end -->