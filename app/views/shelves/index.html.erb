<h1>書櫃列表</h1>
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>名稱</th>
    </tr>
  </thead>

  <tbody>
    <% @shelves.each do |shelf| %>
      <tr>
        <td><%= shelf.id %></td>
        <td><%= link_to shelf.name, shelf_path(shelf) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
